package com.pod2.elevator.scheduling;

/**
 * ElevatorAlgorithms implementations can sub-class this class to associate
 * scheduler-specific data with an Elevator. This data can then be retrieved to
 * make decisions on subsequent schedule calls.
 */
public abstract class SchedulerData {
}
